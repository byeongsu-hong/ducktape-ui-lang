[package]
name = "iced-app"
version = "0.1.0"
edition.workspace = true
publish = false

[dependencies]
iced = { version = "0.14.0", default-features = false, features = ["thread-pool", "tiny-skia", "x11"] }
ui-lang = { path = "../../crates/ui-lang" }

[lints]
workspace = true
