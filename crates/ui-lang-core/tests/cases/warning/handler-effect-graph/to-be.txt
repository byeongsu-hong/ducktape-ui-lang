W006 handler cycle `loaded`, `start` is driven by a future, task, or query completion and can refresh forever
W005 handler `dead` is unreachable from mount, the app view, subscriptions, the tray menu, presets, and tests
W007 unfiltered raw events route redraw requests to `raw` and can create a redraw feedback loop
